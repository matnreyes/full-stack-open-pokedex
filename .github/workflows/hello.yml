name: Hello World!
on:
  push:
    branches:
      - master

jobs:
  hello_word:
    runs-on: ubuntu-20.04
    steps: 
      - name: Say Hello
        run: |
          echo 'Hello World!'
      - name: Date
        run: |
          date
      - name: Directory
        run: |
          ls -l
